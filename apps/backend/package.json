{
  "name": "@monorepo/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --port 8000",
    "bot": "uv run python -m src.bot.main",
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "format": "uv run ruff check --fix . && uv run ruff format .",
    "test": "uv run pytest"
  }
}

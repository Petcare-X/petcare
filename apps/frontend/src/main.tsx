/**
 * ═════════════════════════════════════════════════════════════════════════════
 * Точка входа приложения (Entry Point)
 * ═════════════════════════════════════════════════════════════════════════════
 * 
 * Этот файл:
 * 1. Импортирует необходимые функции и компоненты из React
 * 2. Загружает глобальные стили
 * 3. Находит DOM-элемент для монтирования приложения
 * 4. Инициализирует корневой компонент с поддержкой StrictMode
 * 5. Рендерит приложение в браузер
 */

// ── Импорты ──────────────────────────────────────────────────────────────────
// StrictMode помогает обнаружить потенциальные проблемы в приложении во время разработки
import { StrictMode } from "react";

// createRoot - новый API React 18 для инициализации корневого компонента
import { createRoot } from "react-dom/client";

// Глобальные стили приложения
import "./index.css";

// Главный компонент приложения
import App from "./App";

// ── Инициализация приложения ──────────────────────────────────────────────────
/**
 * Находим DOM-элемент с id="root"
 * В index.html должен быть элемент: <div id="root"></div>
 */
const rootElement = document.getElementById("root");

/**
 * Проверка безопасности: если элемент не найден, выбрасываем ошибку
 * Это предотвращает невидимые ошибки и помогает быстро диагностировать проблемы
 */
if (!rootElement) throw new Error("Root element not found");

/**
 * Создаём корневой React-компонент и монтируем его в DOM
 * 
 * createRoot - начиная с React 18, это рекомендуемый способ инициализации
 * StrictMode - включает дополнительные проверки и предупреждения в разработке
 * <App /> - главный компонент приложения
 */
createRoot(rootElement).render(
  <StrictMode>
    <App />
  </StrictMode>
);
